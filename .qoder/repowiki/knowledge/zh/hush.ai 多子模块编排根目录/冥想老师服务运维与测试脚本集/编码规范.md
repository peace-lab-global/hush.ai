- 测试脚本统一通过 `subprocess.Popen([sys.executable, "-m", "hushai.meditation.app"])` 拉起被测服务，并在 finally 中 terminate+wait 清理进程。
- 对外部依赖缺失（如 WX_APPID、LLM Key）采用优雅降级：跳过该步骤或回退到直连数据库注入测试用户，而非直接失败退出。
- 关键环境变量均以 `MEDITATION_*` 前缀并通过 `os.environ.setdefault` 提供安全默认值，避免运行时抛错。
- 前端质量检查按功能域分节打印（HTML/CSS/JS/功能逻辑/响应式/无障碍/资源/性能），每节用分隔线标题与 ✓/✗/⚠ 符号输出结果。