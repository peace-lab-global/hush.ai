- 通过 `monkeypatch.setenv/delenv` 控制环境变量（如 LLM_APPKEY、HUSH_MODE、HUSH_CALM_MODE），并用 `capsys.readouterr()` 断言 stdout/stderr 输出内容。
- 对外部依赖（OpenAI、系统模块、函数）统一使用 `unittest.mock.patch("hushai.xxx", ...)` 在 with 块内临时替换，避免污染全局状态。
- 涉及 SQLAlchemy 持久化的异步测试使用 `@pytest.mark.asyncio` 装饰器，并依赖 `meditation_session` fixture 提供的内存 SQLite AsyncSession。
- 每个测试文件顶部以三引号注释说明被测模块职责及关键修复点（如 P0 修复、死代码删除），作为行为契约文档。