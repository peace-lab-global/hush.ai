- 所有 Python 代码遵循 ruff (E/F/I/B/UP/SIM) + mypy strict_optional 规范，target-version=py39，line-length=100
- 可选功能按 `[project.optional-dependencies]` 分组（dev / meditation），避免基础包膨胀
- 测试覆盖目标 ≥70%，并通过 `--strict-markers` 强制标记 asyncio 用例
- 覆盖率统计排除 `__main__.py`、migrations、admin pages 与第三方集成文件