SQLAlchemy 2.0 声明式映射（`Mapped`/`mapped_column`）+ `sqlalchemy.ext.asyncio`（AsyncSession/async_sessionmaker）；SQLite via aiosqlite 或 PostgreSQL via asyncpg；ChromaDB 作为轻量向量库，嵌入函数可选 OpenAIEmbeddingFunction；Alembic 异步迁移。